import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div style={{ padding: 40, textAlign: 'center' }}>
            <h1 style={{ color: 'green' }}>Dashboard Debug Mode: Render Success!</h1>
            <p>If you see this, the routing and layout are fine.</p>
        </div>
    );
};

export default Dashboard;
